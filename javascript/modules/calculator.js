function getDom(selector) {
  // document에서 selecor dom 하나 반환시키기
}

function onClick(dom, callback /* 버튼을 클릭했을 때 이벤트 (+ - * /) */) {
  // dom에 click 이벤트 할당하기
}

function getValue(dom) {
  // dom에 입력된 값을 반환하기
}

function setText(dom, text) {
  // dom에 textContent를 할당하기
}
